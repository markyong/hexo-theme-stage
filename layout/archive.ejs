<div class="clearfix">
    <div id="archive">
        <h1><%= page.month + '/' + page.year %></h1>
        <% page.posts.each(function(article) { %>
        <div class="item">
            <h3>
                <time datetime="<%= date_xml(article.date) %>">
                    <%= date(article.date) %>
                </time>
                <% if (article.link) { %>
                <!-- external link -->
                <a href="<%= url_for(article.link) %>" target="_blank">
                    <!--<%= article.title || 'No Title' %>-->
                    <%- truncate(strip_html(article.title), {length: 57, omission: '...'}) %>
                </a>
                <% } else { %>
                <!-- internal link -->
                <a href="<%= url_for(article.path) %>">
                    <%= truncate(strip_html(article.title), {length: 57, omission: '...'}) || 'No Title' %>
                </a>
                <% } %>
            </h3>
        </div>
        <% }); %>
    </div>
    <%- partial('partials/content-aside') %>
</div>
<div id="archive-paginator" class="clearfix">
    <%- paginator({prev_text: '&laquo; ' + __('prev'), next_text: __('next') + ' &raquo;'}) %>
</div>
