<% if (page.posts.length > 0) { %>
  <div id="recent-posts">
    <% page.posts.sort('date', -1).limit(10).each(function (post) { %>
      <div class="post-item">
          <h3>
            <% if (post.date) {%>
            <time datetime="<%= post.date.toJSON() %>">
              <%=date(post.date)%>
            </time>
            <% } %>
            <a href="<%= config.root + post.path %>">
              <%- truncate(strip_html(post.title), {length: 57, omission: '...'}) %>
            </a>
          </h3>
      </div>
    <% }) %>
  </div>
<% } %>
